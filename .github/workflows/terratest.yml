name: terratest

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  validate:
    uses: trussworks/shared-actions/.github/workflows/terratest.yml@create-terratest-workflow
